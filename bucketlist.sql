CREATE TABLE bucketlist (
    id INTEGER,
    name TEXT,
    status TEXT, 
    type TEXT,
    date TEXT,
    PRIMARY KEY(id)
);


-- INSERT INTO bucketlist (name, status) VALUES("Go to Japan", "NOT DONE");

-- UPDATE bucketlist
-- SET status = 'done', date= 'todayddf'
-- WHERE id = ?;